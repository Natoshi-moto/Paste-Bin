# Publication exclusions

Deliberately **not** published in this checkpoint:

- In-progress ChatGPT/Codex worktrees under `/tmp/pa-r0*` (live monkey sessions)
- `relay-work-in-progress/` and partial tarballs from interrupted rounds
- Cloudflare/wrangler credentials, API tokens, `.env`, private keys
- Private conversation transcripts and operator personal vaults
- `pioneer-alignment-public` production deploy secrets
- Any unsealed half-built v15 / Round 024 work

Only **sealed** relay zips and operator-farm control surfaces that contain no credentials.
