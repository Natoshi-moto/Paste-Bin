# NEXUS Assistant pivotal checkpoint 001

**Captured:** `2026-07-25T17:33:51+01:00`  
**Checkpoint status:** `PUBLIC_BACKUP / NONCANONICAL / NOT_A_RELEASE`  
**Status authority:** `NONE`  
**Licence:** MIT, subject to the repository licence and any identified
third-party rights

## Source state

| Field | Value |
|---|---|
| Source repository | `https://github.com/Natoshi-moto/Experimental-Sandbox` |
| Source subdirectory | `projects/Natoshi-Assistant` |
| Source branch | `sandbox/experiment/natoshi-assistant-matrix-terminal` |
| Source HEAD before snapshot | `a564bbef9e18c4707d548d7a5ea11f8387f24d27` |
| Working-tree state | Dirty; this checkpoint deliberately captures uncommitted and untracked project bytes |
| Copied project files | 48 public-safe, non-generated files |
| Bulk/generated exclusions | Python caches, pytest cache, browser build output and bytecode |

The source branch commit alone does not reproduce this checkpoint because the
NEXUS Assistant work was still uncommitted. `MANIFEST.sha256` identifies the
copied source bytes in this public backup.

## Material checkpointed

- compact, terminal-first NEXUS cockpit and blank-system-prompt default;
- provider selection, DeepSeek support, hidden Linux Secret Service key entry,
  thinking-tag separation and lightweight local observer;
- non-interrupting always-on-top notification/cockpit design;
- encrypted Room events, deterministic replay, scoped observer receipts and
  lightweight checkpoints;
- encrypted Greywire-style Drops and single-successor custody evidence;
- LOOM exact-byte encrypted local archive and the DeepSeek-first, distinct
  higher-model Forge railway;
- 25 disabled, credential-free connector declarations and deterministic fixture
  quarantine;
- new offline BitTorrent and MediaWiki/Wikipedia adapter stubs;
- browser-extension/native-host scaffold for Chromium and Firefox;
- design and engineering guides, source audits, threat model, handoff material
  and current result reports;
- GunZ latency/lobby/relay audit, MMO work-exchange synthesis, WinMX/BitTorrent
  transport analysis, Wikipedia revision-source model, and mutated-quine child
  workspace specification.

## Verification observed before publication

```text
Python app suite:
  123 tests passed

Browser native-host suite:
  4 tests passed

Static/build checks:
  Python compilation passed
  shell syntax passed
  JavaScript syntax passed
  Chromium extension build passed
  Firefox extension build passed
  JSON manifests parsed
  git diff --check passed in the source repository
```

The focused connector suite contains 15 passing tests, including negative tests
that keep BitTorrent DHT/PEX/seeding and MediaWiki editing/content execution
unavailable.

## Verify the copied bytes

From this checkpoint directory:

```bash
sha256sum -c MANIFEST.sha256
```

The manifest covers `source/Natoshi-Assistant/**`, not this explanatory file or
the manifest itself.

## Honest limits

- This is a public backup of a development state, not a secure P2P release.
- The installed/running desktop copy was not updated by this checkpoint.
- No live BitTorrent, WinMX, MediaWiki, Nostr, IRC, radio, satellite, Tails, or
  other future connector was enabled.
- No unrestricted sudo/root controller exists.
- The Forge can form a commit proposal but does not execute Git or publish by
  itself.
- No credentialed DeepSeek-to-higher-model live Forge run is claimed.
- A hash proves byte agreement with this chosen snapshot, not authorship,
  malware absence, semantic truth, legal finality, or universal consensus.
- Raw conversation history is not represented as complete or verbatim here.
  Public session export needs its own explicit capture, privacy review, and
  scrubbed/public projection receipt.

## Lineage

The detailed design/source lineage is inside
[`source/Natoshi-Assistant/NEXUS_ROOM_LOOM_CONNECTIVITY_SPEC.md`](source/Natoshi-Assistant/NEXUS_ROOM_LOOM_CONNECTIVITY_SPEC.md).
This checkpoint preserves that source trail without promoting it to canon.
